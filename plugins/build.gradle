// importion the archiving.gradle 
apply from : "archiving.gradle"


//can also import binary plugins in this fasion, this would have to be donwloaded from plugins.gradle.org
plugins {
    id "com.dorongold.task-tree" version "2.1.0"
}